#### étape 1 : Bronze : donnée brute

on a téléversé les données brute dans le warehouse duckdb, j'ai découvert les function fetchall() et fetchone() de duckdb qui récupère les résultats des requêtes sql dde la mémoire.